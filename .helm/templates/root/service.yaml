---
apiVersion: v1
kind: Service
metadata:
  name: vault-root
  namespace: {{ .Values.namespace }}
  labels:
    app: vault-root
spec:
  clusterIP: None
  ports:
  - name: root
    port: 8200
    protocol: TCP
    targetPort: 8200
  selector:
    app: vault-root
